# Ingest / OCR layer
PyMuPDF>=1.24        # native PDF text extraction + page rasterization
pytesseract>=0.3.10  # OCR (requires the tesseract binary installed separately)
Pillow>=10.3         # image handling for OCR
pydantic>=2.7
pydantic-settings>=2.3

# Table & section detection (Layer 2)
pdfplumber>=0.11           # native PDF table/word extraction
rapidfuzz>=3.9             # fuzzy keyword matching (local, free)
sentence-transformers>=3.0 # local embeddings for classification (free, no API)
numpy>=1.26

# GPT structuring + insights (Layer 3)
openai>=1.40

# API (Layer 7)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Dev / test
pytest>=8.2
httpx>=0.27          # FastAPI TestClient
